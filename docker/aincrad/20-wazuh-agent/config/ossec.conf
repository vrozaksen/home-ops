<!--
  Wazuh agent - aincrad (Unraid).
  The manager's LoadBalancer IP is reachable from the Servers VLAN because its
  Service opts into Cilium L2 announcements; without that a BGP-only IP cannot
  be ARPed from this subnet and the agent would be pinned to one node's port.
-->
<ossec_config>
  <client>
    <server>
      <address>10.10.0.93</address>
      <port>1514</port>
      <protocol>tcp</protocol>
    </server>
    <!-- The manager's agents_disconnection_time is 20s, so anything slower than
         this leaves the agent counted as disconnected between keep-alives. -->
    <notify_time>10</notify_time>
    <time-reconnect>60</time-reconnect>
    <auto_restart>yes</auto_restart>
    <crypto_method>aes</crypto_method>
    <enrollment>
      <enabled>yes</enabled>
      <manager_address>10.10.0.93</manager_address>
      <port>1515</port>
      <agent_name>aincrad</agent_name>
      <!-- Compose places secrets under /run/secrets regardless of target. -->
      <authorization_pass_path>/run/secrets/authd_pass</authorization_pass_path>
    </enrollment>
  </client>

  <client_buffer>
    <disabled>no</disabled>
    <queue_size>5000</queue_size>
    <events_per_second>500</events_per_second>
  </client_buffer>

  <!-- Unraid keeps / on a RAM disk rebuilt at boot, so only persistent paths
       are worth watching: the flash config and the appdata pool. -->
  <syscheck>
    <disabled>no</disabled>
    <frequency>43200</frequency>
    <scan_on_start>yes</scan_on_start>
    <alert_new_files>yes</alert_new_files>

    <!-- Yield to everything else on the NAS: lowest scheduling priority and a
         capped event rate, so a scan degrades rather than starves the host. -->
    <process_priority>19</process_priority>
    <max_eps>50</max_eps>

    <!-- No report_changes anywhere: the image ships no `diff`, so it only
         emits error 6714 on every change it detects. -->
    <!-- Shares, users, SSH host keys, plugin state. -->
    <directories check_all="yes" realtime="yes">/boot/config</directories>
    <!-- App configs and the *.vzkn.eu wildcard private key. -->
    <directories check_all="yes" realtime="yes">/mnt/blaze/appdata/caddy</directories>

    <!-- appdata as a whole is deliberately NOT watched. It is mostly
         application payload -- node_modules, caches, databases -- which hit the
         100k entry limit on tdarr alone and took the host down within minutes
         of every scan. Add individual config paths here instead. -->

    <!-- Belt and braces, in case a watched path ever grows one of these. -->
    <ignore type="sregex">/node_modules/|/\.git/|/cache/|/logs?/</ignore>

    <!-- Churn with no security value. -->
    <ignore type="sregex">\.log$|\.tmp$|\.db-journal$|\.sqlite-wal$|\.sqlite-shm$</ignore>
    <ignore>/mnt/blaze/appdata/doco-cd/data</ignore>
    <ignore>/boot/config/plugins/dynamix/dynamix.cfg</ignore>

    <skip_nfs>yes</skip_nfs>
    <skip_dev>yes</skip_dev>
    <skip_proc>yes</skip_proc>
    <skip_sys>yes</skip_sys>
  </syscheck>

  <rootcheck>
    <disabled>no</disabled>
    <check_files>yes</check_files>
    <check_trojans>yes</check_trojans>
    <check_dev>yes</check_dev>
    <check_sys>yes</check_sys>
    <!-- Off: without the host PID namespace these see only the container. -->
    <check_pids>no</check_pids>
    <check_ports>no</check_ports>
    <check_if>yes</check_if>
    <frequency>43200</frequency>
  </rootcheck>

  <!-- Only what a containerised agent can report truthfully about the host.
       hardware reads /proc and /sys, which are the host's. packages, ports,
       processes and network would describe this container instead -- and
       package data would make Vulnerability Detection raise CVEs for the Wazuh
       image rather than for Unraid. -->
  <wodle name="syscollector">
    <disabled>no</disabled>
    <interval>1h</interval>
    <scan_on_start>yes</scan_on_start>
    <hardware>yes</hardware>
    <os>yes</os>
    <network>no</network>
    <packages>no</packages>
    <ports all="no">no</ports>
    <processes>no</processes>
  </wodle>

  <!-- Unraid keeps syslog in RAM only, so these exist only while the box is up.
       Enabling its syslog server would make this materially more useful. -->
  <localfile>
    <log_format>syslog</log_format>
    <location>/var/log/syslog</location>
  </localfile>

  <logging>
    <log_format>plain</log_format>
  </logging>
</ossec_config>
