---
# yaml-language-server: $schema=https://schemas.vzkn.eu/externaldns.k8s.io/dnsendpoint_v1alpha1.json
apiVersion: externaldns.k8s.io/v1alpha1
kind: DNSEndpoint
metadata:
  name: bluesky
spec:
  endpoints:
    - dnsName: bluesky.vzkn.eu
      recordType: CNAME
      targets:
        - "tunnel.vzkn.eu"
      # ATproto DID document (PLC) - placeholder. After PDS bootstrap, retrieve real DID
      # from cocoon logs and update target. Required for federation.
      # - dnsName: _atproto.vzkn.eu
      #   recordType: TXT
      #   targets:
      #     - "did=did:plc:<replace-with-actual-did>"
