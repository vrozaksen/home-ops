---
# yaml-language-server: $schema=https://schemas.vzkn.eu/toolhive.stacklok.dev/mcpserver_v1alpha1.json
apiVersion: toolhive.stacklok.dev/v1alpha1
kind: MCPServer
metadata:
  name: sentry
spec:
  image: registry.vzkn.eu/containers/sentry-mcp:0.36.0@sha256:c6845cfc777c0c2cfd24bab4d055e32ce36d2548d78594269286176a2b9723ba
  transport: stdio
  proxyPort: 8080
  groupRef:
    name: mcp-tools
  permissionProfile:
    type: builtin
    name: network
  env:
    - name: SENTRY_HOST
      value: sentry.vzkn.eu
    # SaaS-only skills: seer (AI debugger, commercial Sentry only),
    # preprod (CI visual regression snapshots, SaaS feature).
    - name: MCP_DISABLE_SKILLS
      value: seer,preprod
  secrets:
    - name: sentry-mcp-secret
      key: SENTRY_AUTH_TOKEN
      targetEnvName: SENTRY_ACCESS_TOKEN
