# Import existing buckets
import {
  to = module.buckets["cnpg"].garage_bucket.bucket
  id = "cnpg"
}

import {
  to = module.buckets["forgejo"].garage_bucket.bucket
  id = "forgejo"
}

import {
  to = module.buckets["harbor"].garage_bucket.bucket
  id = "harbor"
}

import {
  to = module.buckets["sentry"].garage_bucket.bucket
  id = "sentry"
}

import {
  to = module.buckets["tfstate-mikrotik-terraform"].garage_bucket.bucket
  id = "tfstate-mikrotik-terraform"
}

import {
  to = module.buckets["rresume"].garage_bucket.bucket
  id = "rresume"
}
