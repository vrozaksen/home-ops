---
# The Role and RoleBinding that let this account exec into the toolbox live in
# rook-ceph/cluster, because this Kustomization rewrites every namespace to
# system-upgrade and would drag them out of the namespace they have to bind in.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: tuppr-ceph-hook
  namespace: system-upgrade
