---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: ai
components:
  - ../../components/common
resources:
  - ./namespace.yaml
  - ./agentmemory/ks.yaml
  - ./text-embeddings-inference/ks.yaml
  - ./toolhive/ks.yaml
  # MCP servers — Group 1 (upstream images / remote proxies)
  - ./mcp/siderolabs-docs/ks.yaml
  # - ./mcp/sentry/ks.yaml
  - ./mcp/context7/ks.yaml
  - ./mcp/kubernetes/ks.yaml
  # MCP servers — Group 2 (own builds in ~/git/containers)
  - ./mcp/agentmemory/ks.yaml
  - ./mcp/flux-operator/ks.yaml
  - ./mcp/forgejo/ks.yaml
  - ./mcp/nixos/ks.yaml
  - ./mcp/talos/ks.yaml
