---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ./db
  - ./helmrelease.yaml
patches:
  # Patch the component-rendered Cluster (from ./db) to add pgvector.
  - path: ./patches/cluster-pgvector.yaml
