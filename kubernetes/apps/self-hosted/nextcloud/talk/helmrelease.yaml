---
# yaml-language-server: $schema=https://raw.githubusercontent.com/bjw-s-labs/helm-charts/main/charts/other/app-template/schemas/helmrelease-helm-v2.schema.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: nextcloud-talk-signaling
spec:
  interval: 1h
  chartRef:
    kind: OCIRepository
    name: app-template
  values:
    controllers:
      signaling:
        strategy: RollingUpdate
        containers:
          app:
            image:
              repository: ghcr.io/strukturag/nextcloud-spreed-signaling
              tag: 2.1.0@sha256:190f70b41db10dd6594db166e27f64c40cb31c5a98910ae1c99d4245977b199b
            env:
              SIGNALING_LISTEN: 0.0.0.0:8080
            probes:
              liveness:
                enabled: true
                custom: true
                spec:
                  tcpSocket:
                    port: 8080
                  initialDelaySeconds: 10
                  periodSeconds: 10
              readiness:
                enabled: true
                custom: true
                spec:
                  tcpSocket:
                    port: 8080
                  initialDelaySeconds: 5
                  periodSeconds: 5

    service:
      app:
        controller: signaling
        ports:
          http:
            port: 8080

    persistence:
      config:
        type: secret
        name: nextcloud-talk-secret
        globalMounts:
          - path: /config/server.conf
            subPath: server.conf
            readOnly: true
