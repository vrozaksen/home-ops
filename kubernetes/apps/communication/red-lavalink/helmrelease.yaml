---
# yaml-language-server: $schema=https://raw.githubusercontent.com/bjw-s-labs/helm-charts/main/charts/other/app-template/schemas/helmrelease-helm-v2.schema.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: red-lavalink
spec:
  interval: 1h
  chartRef:
    kind: OCIRepository
    name: app-template
  values:
    controllers:
      red-lavalink:
        replicas: 1
        strategy: Recreate
        containers:
          app:
            image:
              repository: ghcr.io/vrozaksen/red-lavalink
              tag: 3.7.13@sha256:59f79806d959e859f2fbd5e712631c06e4637153ed47c5661095055438f64127
            env:
              JAVA_OPTS: "-Xmx4G"
              PUID: &uid 1000
              PGID: &gid 100
              SERVER_PORT: &port 2333
            envFrom:
              - secretRef:
                  name: red-lavalink-secret
            probes:
              startup:
                enabled: true
                custom: true
                spec:
                  tcpSocket:
                    port: *port
                  initialDelaySeconds: 10
                  periodSeconds: 10
                  timeoutSeconds: 1
                  failureThreshold: 30
              liveness:
                enabled: false
              readiness:
                enabled: false
            securityContext:
              allowPrivilegeEscalation: false
              readOnlyRootFilesystem: false
              capabilities: {drop: ["ALL"]}
            resources:
              requests:
                cpu: 200m
                memory: 2Gi
              limits:
                memory: 5Gi
    defaultPodOptions:
      securityContext:
        runAsNonRoot: true
        runAsUser: *uid
        runAsGroup: *gid
        fsGroup: *gid
        fsGroupChangePolicy: OnRootMismatch
    service:
      red-lavalink:
        controller: red-lavalink
        ports:
          http:
            port: *port
    persistence:
      data:
        type: emptyDir
